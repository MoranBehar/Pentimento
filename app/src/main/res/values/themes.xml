<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.Pentimento" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_background</item>
        <item name="android:windowBackground">@color/primary_background</item>
    </style>

    <!-- Style for my Pentimento project-->
    <style name="AppTheme" parent="Base.Theme.Pentimento">
        <item name="android:alertDialogTheme">@style/DefaultDialogStyle</item>
    </style>

</resources>